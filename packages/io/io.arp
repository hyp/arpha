# TODO
import cstdlib

def print(string Range(char8)){
	until(string.empty){
		putchar( string.current as int32 )
		string.moveNext()
	}
}

# These are temporary hacks:
macro print(a,b) = [>
	print($a)
	print($b)
<]
macro print(a,b,c) = [>
	print($a)
	print($b)
	print($c)
<]
