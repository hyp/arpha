import io, algorithms

var array = [0,1,2,3,4,5]

def main()
{
	var r = array.find(2)
	if(!r.empty) print("found 2!")
	
	array.replace(2,42)
	r = array.find(42)
	if(!r.empty) print("found 42!")
	
	def n = array.count(0)
	if(n == 1) print("There is one zero!")
}
